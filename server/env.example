# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dorama_alem
DB_USER=postgres
DB_PASSWORD=your_password

# Firebase Admin SDK
# Download your Firebase service account key and place it in the server directory
# Then set the path here
FIREBASE_SERVICE_ACCOUNT_KEY=./firebase-service-account-key.json

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

